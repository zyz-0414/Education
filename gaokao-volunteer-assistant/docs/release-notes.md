# Release Notes

## Unreleased

- 初始化项目准备骨架。
- 固定第一版范围和技术栈。
- 新增数据目录、CSV 模板和文档体系。
- 新增 Prisma schema 草案。
- 完成第 1-2 周安徽首批数据整理，包含 2024/2025 一分一段完整 CSV、普通本科批投档结果 OCR 首批样例、2025 招生计划样例和具体来源索引。
- 将院校专业组、招生计划、投档结果的数据结构补充 `subject_track` 字段。
- 完成第 3 周数据库和导入：新增初始迁移、Prisma 入库脚本、CSV 完整性校验和院校专业组查询 API。
- 完成第 4 周考生建档和规则过滤：新增候选集接口、分数位次校验、选科过滤和首页建档工作台。
- 完成第 5 周推荐算法：新增推荐接口、冲稳保分档、偏好排序和低置信度标记。
- 完成第 6 周推荐结果页：新增桌面表格、手机卡片、风险标签、推荐理由和院校专业组详情入口。
- 修复不同年份院校代码复用导致的推荐展示名称误配，并将正式推荐默认限制为有招生计划明细的数据。
- 完成第 6 周数据一致性审计：新增院校名称快照守卫、历史匹配隔离、专业组搜索修正、OCR 名称脏值修复和高危项默认隐藏。
- 完成第 7 周志愿表编辑器：支持加入、删除、上移/下移排序、45 个志愿限制、冲稳保比例提示和浏览器本地方案保存。
- 接入 `D:\education\data` 中的安徽收集版 Excel 数据，覆盖旧样例 cleaned CSV，新增全量招生计划、专业组投档线、2023 旧文理科院校级参考位次和数据烟测。
- 将 CSV 入库从逐行 upsert 改为批量替换，支持 47,401 条招生计划和 10,758 条投档结果快速导入。
- 推荐模型升级为近三年参考位次：2024/2025 优先使用同专业组线、缺失时使用同院校相近专业组补充，2023 理科映射物理类、文科映射历史类，并作为旧文理科院校线低权重参与。
- 调整推荐算法：候选池扩展到 5000 条，按高危/冲/稳/保/过保配额生成推荐，冲刺阈值改为按位次段自适应，并开放高危冲刺、过保兜底开关。
- 修正推荐展示顺序：高危排在冲前面，同一档内按参考位次从小到大展示；候选挑选排序和最终展示排序分离。
- 优化工作台侧栏：左侧画像表单和右侧详情面板在桌面布局下支持独立纵向滚动。
- 完成第 8 周风险检测和报告：新增滑档、保底不足、排斥专业、高学费、低置信度检测，首页展示简版报告摘要，新增 `/report` 简版方案报告页面，并支持导出包含志愿表和分析报告的 PDF。
- 完成第 9 周上线准备：页面正式收口为“安徽高考志愿助手”，新增 Vercel 部署配置、Neon PostgreSQL 部署说明、生产迁移命令、中文 PDF 字体和 `npm run verify:week9`。
